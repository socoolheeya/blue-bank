apiVersion: v1
kind: Service
metadata:
  name: blue-bank-loan
  namespace: blue-bank
spec:
  type: ClusterIP
  selector:
    app: blue-bank-loan
  ports:
    - name: http
      port: 8300
      targetPort: http